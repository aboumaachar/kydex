# -----------------------------------------------------------------------
# Sandra Nassif Kallab – notary page at /sandra/
# Route through cPanel Apache/PHP backend for stable PHP execution.
# -----------------------------------------------------------------------

location = /sandra {
    return 301 /sandra/;
}

location ^~ /sandra/ {
    include conf.d/includes-optional/cpanel-proxy.conf;
    proxy_pass $CPANEL_APACHE_PROXY_PASS;
}
