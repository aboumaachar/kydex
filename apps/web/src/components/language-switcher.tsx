"use client";

import { useI18n, type Language } from '../i18n/i18n-provider';

export default function LanguageSwitcher() {
  // Language switching is intentionally hidden when forcing Arabic-only UI.
  return null;
}