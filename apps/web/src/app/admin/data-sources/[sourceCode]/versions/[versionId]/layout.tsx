export const dynamicParams = false;

export function generateStaticParams() {
	return [];
}

export default function DataSourceVersionLayout({ children }: Readonly<{ children: React.ReactNode }>) {
	return children;
}