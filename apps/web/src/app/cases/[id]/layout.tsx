export const dynamicParams = false;

export function generateStaticParams() {
	return [];
}

export default function CaseDetailLayout({ children }: Readonly<{ children: React.ReactNode }>) {
	return children;
}